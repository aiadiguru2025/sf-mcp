# SF-MCP Configuration
# Copy this file to .env and fill in your values

# API host (optional, defaults to api55preview.sapsf.eu)
SF_API_HOST=api55preview.sapsf.eu

# MCP API Key for endpoint authentication (optional)
# MCP_API_KEY=your_api_key

# Note: SF_USER_ID and SF_PASSWORD are NOT supported.
# Credentials must be provided as tool parameters on each request.
